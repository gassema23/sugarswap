/**
 * Icon — thin re-export of GameIcon with a cleaner import path.
 * Use this for all new code: `import Icon from '@/shared/components/Icon'`
 */
export { default, type SpriteIconId, rankSprite } from '../../components/GameIcon';

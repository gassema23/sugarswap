# Core RL dependencies for Sugar Swap AI training
gymnasium>=0.29.0
numpy>=1.24.0

# Optional but recommended
stable-baselines3>=2.2.0   # PPO / DQN algorithms
sb3-contrib>=2.2.0         # MaskablePPO (action masking during training)
tensorboard>=2.14.0        # Training visualisation
torch>=2.0.0               # PyTorch backend for SB3
onnxscript>=0.1.0          # Required by torch.onnx.export in PyTorch ≥ 2.4
onnx>=1.16.0               # Merge external .onnx.data back into single file
onnxruntime>=1.18.0        # Runtime for .onnx inference + export --test
